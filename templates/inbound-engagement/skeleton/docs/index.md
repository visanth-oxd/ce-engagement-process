# ${{ values.clientName }} — ${{ values.name }}

${{ values.summary }}

| Field | Value |
|---|---|
| Requested by | ${{ values.requestedBy }} |
| Requested date | ${{ values.requestedDate }} |
| Nature | ${{ values.requestNature }} |
| Jira | ${{ values.jiraKey }} |

Live workflow state, impact assessments, and environment allocation are on the
[engagement entity page](/catalog/default/engagement/${{ values.name }}).
This page is for engagement-specific notes, decisions, and scoping detail —
keep it updated as triage and IAs progress.
