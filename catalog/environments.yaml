---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: bld-environment
  title: BLD (Build)
  description: Build environment. Contended — allocation managed via engagement triage.
  tags: [environment]
spec:
  type: environment
  owner: group:default/sre-run
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: int-environment
  title: INT (Integration)
  description: Integration test environment. Allocation visible via Engagement dependsOn relations.
  tags: [environment]
spec:
  type: environment
  owner: group:default/sre-run
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: pre-environment
  title: PRE (Pre-production)
  description: Pre-production environment. NFT runs here — allocation triggers NFT notification.
  tags: [environment]
spec:
  type: environment
  owner: group:default/sre-run
